 'use client'

import styled from 'styled-components'

export const StyledSitiesList = styled.div`
   display: flex;
   margin-bottom: 20px;

   & > button:nth-child(n) {
    border: 1px solid;
    padding: 3px;
    border-radius: 2px;
    margin-right: 10px;
  }
  }
`
